syntax = "v1"

info (
	title: "Dashboard"
	desc: "APIs in Dashboard"
	author: "v3nooom"
	email: "v3nooom@outlook.com"
)

@server(
	group: dashboard
	prefix: dashboard
)
service bff-api {
	@handler HumanServer
  	get / returns (HumServerResp)
}

// HumanServer
type (
	HumServerResp {
		Up string `json:"up"`
	}
)
