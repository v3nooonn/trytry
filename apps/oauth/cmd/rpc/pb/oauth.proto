syntax = "proto3";

package oauth;
option go_package="./oauth";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Oauth {
  rpc Ping(Request) returns(Response);
}
